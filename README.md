# pg_ext_memcheck
A PostgreSQL extension that instruments and stress-tests another extension's memory behaviour from inside the backend process.

## To-Do
- [] Initialize the setup and required files for a PostgreSQL extension.
- [] Start small and simple, and then build up the functionality iteratively.
- [] Build a simple extension that can be loaded into PostgreSQL and can perform basic memory checks.
- [] Implement the core functionality of the extension to instrument and stress-test another extension's memory behaviour.
- [] Test the extension thoroughly to ensure it works as expected and does not cause any issues with PostgreSQL.
- [] Add Regression tests to ensure that the extension does not break existing functionality in PostgreSQL.
- [] Update the README and documentation to provide clear instructions on how to use the extension and its features.